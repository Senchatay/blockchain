# CLI BLOCKCHAIN для Транзакций
- Клонирование репозитория:
```bash
git clone git@github.com:Senchatay/blockchain.git
```
- Запуск в терминале (С помощью `docker-compose`):
```bash
docker-compose build
docker-compose run --rm app
```